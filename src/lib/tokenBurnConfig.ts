/**
 * Token Burn Configuration
 *
 * Defines how many tokens each action costs.
 * AI Coach does NOT accept or track videos/images for assessment.
 *
 * Philosophy:
 *   - Chat is cheap (encourage engagement)
 *   - Full AI builds are the main token sink
 *   - Manual tools are free (no AI = no tokens)
 *   - Uni courses are premium one-time purchases (20 tokens L2/L3, 50 tokens L4)
 *   - UnTunes music: free streaming for paid tiers, optional purchases with tokens
 *
 * Tier tokens:  Foundation 1,000/month
 * 1 token = 1p.  Top-up: £10 = 1,000 tokens (never expire)
 */

import type { TierKey } from './subscriptionTiers';

export type ActionCategory = 'free' | 'chat' | 'ai_build' | 'ai_analysis' | 'course' | 'bundle' | 'music';

export interface TokenAction {
  id: string;
  name: string;
  description: string;
  category: ActionCategory;
  baseCost: number; // tokens (0 = free)
  /** Some actions cost less on higher tiers */
  tierDiscount?: Partial<Record<TierKey, number>>; // multiplier (0.8 = 20% off)
}

// ─── TOKEN ACTIONS ───

export const TOKEN_ACTIONS: Record<string, TokenAction> = {
  // ─── FREE (no tokens) ───
  manual_tracker: {
    id: 'manual_tracker',
    name: 'Manual tracking',
    description: 'Log workouts, food, water, habits manually',
    category: 'free',
    baseCost: 0,
  },
  calculator: {
    id: 'calculator',
    name: 'Calculators',
    description: 'BMI, TDEE, 1RM, macro calculators',
    category: 'free',
    baseCost: 0,
  },
  social_feed: {
    id: 'social_feed',
    name: 'Social feed',
    description: 'Browse, post, like, comment',
    category: 'free',
    baseCost: 0,
  },
  habit_tracking: {
    id: 'habit_tracking',
    name: 'Habit tracking',
    description: 'Daily 7 habits, streaks, water tracker',
    category: 'free',
    baseCost: 0,
  },
  browse_exercises: {
    id: 'browse_exercises',
    name: 'Browse exercises',
    description: 'View exercise library (viewing is free)',
    category: 'free',
    baseCost: 0,
  },
  university_l1: {
    id: 'university_l1',
    name: 'University Level 1',
    description: 'Free introductory courses for all pillars',
    category: 'free',
    baseCost: 0,
  },
  profile_timeline: {
    id: 'profile_timeline',
    name: 'Profile & timeline',
    description: 'Profile page, timeline feed, post updates',
    category: 'free',
    baseCost: 0,
  },
  notifications: {
    id: 'notifications',
    name: 'Notifications',
    description: 'Kudos, comments, follows, friend requests',
    category: 'free',
    baseCost: 0,
  },
  messaging: {
    id: 'messaging',
    name: 'Messaging',
    description: 'Direct messages between users',
    category: 'free',
    baseCost: 0,
  },

  // ─── CHAT (lightweight, encourage usage) ───
  coach_chat: {
    id: 'coach_chat',
    name: 'Coach chat message',
    description: 'Ask your Unbreakable Coach anything (text only — no video/image)',
    category: 'chat',
    baseCost: 10,
    tierDiscount: { elite: 0.6 },
  },
  motivation_quote: {
    id: 'motivation_quote',
    name: 'Motivation & mindset',
    description: 'Daily motivation, affirmations, mindset coaching tips',
    category: 'chat',
    baseCost: 0,
  },
  progression_tip: {
    id: 'progression_tip',
    name: 'Progression suggestion',
    description: 'Quick tips on form, progression, recovery',
    category: 'chat',
    baseCost: 5,
  },

  // ─── AI BUILDS (the main token sink) ───
  programme_build: {
    id: 'programme_build',
    name: 'AI programme build',
    description: 'Full personalised workout programme generated by AI',
    category: 'ai_build',
    baseCost: 50,
    tierDiscount: { elite: 0.6 },
  },
  meal_plan: {
    id: 'meal_plan',
    name: 'AI meal plan',
    description: 'Personalised nutrition / meal plan generated by AI',
    category: 'ai_build',
    baseCost: 50,
    tierDiscount: { elite: 0.6 },
  },
  u86_programme: {
    id: 'u86_programme',
    name: 'UNBREAKABLE 86 plan',
    description: 'AI-generated personalised 86-day programme',
    category: 'ai_build',
    baseCost: 75,
    tierDiscount: { pro: 0.8, elite: 0.6 },
  },

  // ─── AI ANALYSIS ───
  workout_feedback: {
    id: 'workout_feedback',
    name: 'Workout review',
    description: 'AI coach reviews your logged session and gives feedback',
    category: 'ai_analysis',
    baseCost: 15,
  },
  nutrition_analysis: {
    id: 'nutrition_analysis',
    name: 'Nutrition analysis',
    description: 'AI analysis of your food log with recommendations',
    category: 'ai_analysis',
    baseCost: 15,
  },
  progress_report: {
    id: 'progress_report',
    name: 'Progress report',
    description: 'AI-generated weekly/monthly progress summary',
    category: 'ai_analysis',
    baseCost: 30,
    tierDiscount: { elite: 0.5 },
  },
  ai_exercise_search: {
    id: 'ai_exercise_search',
    name: 'AI exercise search',
    description: 'Intelligent exercise library search with AI recommendations',
    category: 'ai_analysis',
    baseCost: 10,
  },

  // ─── UNI COURSES (one-time purchase, lifetime access) ───
  // All courses L2-L4: 50 tokens each (£20 equivalent)
  // Must complete previous level to unlock next: L1(free) → L2 → L3 → L4
  course_l2: {
    id: 'course_l2',
    name: 'Level 2 course',
    description: 'Any single Level 2 university course — lifetime access',
    category: 'course',
    baseCost: 50,
  },
  course_l3: {
    id: 'course_l3',
    name: 'Level 3 course',
    description: 'Any single Level 3 university course — lifetime access. Requires Level 2 completion.',
    category: 'course',
    baseCost: 50,
  },
  course_l4: {
    id: 'course_l4',
    name: 'Level 4 course',
    description: 'Any Level 4 university course — lifetime access. Requires Level 3 completion.',
    category: 'course',
    baseCost: 50,
  },
  course_sport: {
    id: 'course_sport',
    name: 'L4 Sport-specific course',
    description: 'Any sport-specific Level 4 course — lifetime access. Requires Level 3 completion.',
    category: 'course',
    baseCost: 50,
  },
  // Backward-compatible ID
  course_individual: {
    id: 'course_individual',
    name: 'Individual course',
    description: 'Any single university course (L2, L3, L4) — lifetime access',
    category: 'course',
    baseCost: 50,
  },

  // ─── PB Card purchases ───
  card_image: {
    id: 'card_image',
    name: 'PB Card image download',
    description: 'Download your PB card as a high-quality image file',
    category: 'course' as ActionCategory,
    baseCost: 25,
  },
  card_video: {
    id: 'card_video',
    name: 'PB Card video download',
    description: 'Download your PB card as a video file with rarity animation',
    category: 'course' as ActionCategory,
    baseCost: 40,
  },

  // ─── Un-Tunes music store ───
  untunes_single: {
    id: 'untunes_single',
    name: 'Un-Tunes single',
    description: 'Buy a single track + collectible card',
    category: 'music',
    baseCost: 25,
  },
  untunes_album: {
    id: 'untunes_album',
    name: 'Un-Tunes album',
    description: 'Buy a full album + all track cards + album card',
    category: 'music',
    baseCost: 200,
  },
  untunes_bundle: {
    id: 'untunes_bundle',
    name: 'Un-Tunes bundle',
    description: 'All albums bundle — price of 2, boosted rare odds',
    category: 'bundle',
    baseCost: 50,
  },
} as const;

// ─── BUNDLE PRICING (token costs) ───

export interface TokenBundle {
  id: string;
  name: string;
  description: string;
  tokenCost: number;
  savings: string; // human-readable savings
  courses: string[]; // course keys included
}

export const COURSE_BUNDLES: TokenBundle[] = [
  {
    id: 'power_l234',
    name: 'Power L2 + L3 + L4',
    description: 'Complete Power pillar (Levels 2, 3 & 4)',
    tokenCost: 120,
    savings: 'Save 30 tokens vs buying individually',
    courses: ['gym_l2', 'gym_l3', 'gym_l4'],
  },
  {
    id: 'fuel_l234',
    name: 'Fuel L2 + L3 + L4',
    description: 'Complete Fuel pillar (Levels 2, 3 & 4)',
    tokenCost: 120,
    savings: 'Save 30 tokens vs buying individually',
    courses: ['nutrition_l2', 'nutrition_l3', 'nutrition_l4'],
  },
  {
    id: 'mindset_l234',
    name: 'Mindset L2 + L3 + L4',
    description: 'Complete Mindset pillar (Levels 2, 3 & 4)',
    tokenCost: 120,
    savings: 'Save 30 tokens vs buying individually',
    courses: ['mindset_l2', 'mindset_l3', 'mindset_l4'],
  },
  {
    id: 'mega_bundle',
    name: 'Mega Bundle',
    description: 'All 3 pillars, all levels (Power + Fuel + Mindset L2-L4)',
    tokenCost: 300,
    savings: 'Save 150 tokens vs buying individually',
    courses: [
      'gym_l2', 'gym_l3', 'gym_l4',
      'nutrition_l2', 'nutrition_l3', 'nutrition_l4',
      'mindset_l2', 'mindset_l3', 'mindset_l4',
    ],
  },
];

// ─── TOKEN TOP-UPS ───

export interface TokenTopUp {
  id: string;
  name: string;
  tokens: number;
  price: number; // GBP
  stripePriceId: string | null; // Will be created in Stripe
  valuePerToken: string; // human-readable
  popular?: boolean;
}

export const TOKEN_TOPUPS: TokenTopUp[] = [
  {
    id: 'topup_standard',
    name: 'Token Top-Up',
    tokens: 1000,
    price: 10,
    stripePriceId: 'price_1TaPmmD5KOEmeWH2lbJWYqDf',
    valuePerToken: '1p/token',
    popular: true,
  },
];

// ─── HELPERS ───

/** Calculate the actual token cost for an action given user's tier */
export function getActionCost(actionId: string, userTier: TierKey): number {
  const action = TOKEN_ACTIONS[actionId];
  if (!action) return 0;
  if (action.baseCost === 0) return 0;

  const discount = action.tierDiscount?.[userTier] ?? 1;
  return Math.round(action.baseCost * discount * 100) / 100; // round to 2dp
}

/** Get all free actions */
export function getFreeActions(): TokenAction[] {
  return Object.values(TOKEN_ACTIONS).filter(a => a.category === 'free');
}

/** Get all paid actions grouped by category */
export function getPaidActionsByCategory(): Record<ActionCategory, TokenAction[]> {
  const result: Record<ActionCategory, TokenAction[]> = {
    free: [],
    chat: [],
    ai_build: [],
    ai_analysis: [],
    course: [],
    bundle: [],
    music: [],
  };

  for (const action of Object.values(TOKEN_ACTIONS)) {
    result[action.category].push(action);
  }

  return result;
}

/** Calculate token usage percentage */
export function getTokenUsagePercent(used: number, total: number): number {
  if (total <= 0) return 0;
  return Math.min(100, Math.round((used / total) * 100));
}

/** Check if user should see upgrade nudge (≥90% used) */
export function shouldShowUpgradeNudge(used: number, total: number): boolean {
  if (total <= 0) return false;
  return (used / total) >= 0.9;
}
